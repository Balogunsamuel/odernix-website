import { Inter, Roboto_Mono, Cormorant_Garamond, Montserrat, Cinzel} from 'next/font/google';

export const inter = Inter({
    subsets: ['latin'],
    display: 'swap',
});

export const roboto_mono = Roboto_Mono({
    subsets: ['latin'],
    display: 'swap',
});

export const comorant_garamond = Cormorant_Garamond({
    subsets: ['latin'],
    weight: "400"
});

export const montserrat = Montserrat({
    subsets: ['latin'],
    weight: "300"
});
export const montserrat_bold = Montserrat({
    subsets: ['latin'],
    weight: "600"
});